apiVersion: v1
kind: Service
metadata:
  name: {{ include "fraud-detection-engine.fullname" . }}
  labels:
    {{- include "fraud-detection-engine.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  selector:
    {{- include "fraud-detection-engine.selectorLabels" . | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
